export const UP = `UP`;
export const DOWN = `DOWN`;
export const LEFT = `LEFT`;
export const RIGHT = `RIGHT`;
export const SHOOT = `SHOOT`;
export const JUMP = `JUMP`;

export default {
  UP, DOWN, LEFT, RIGHT, SHOOT, JUMP,
};